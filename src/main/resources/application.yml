spring:
  datasource:
    url: jdbc:mysql://database-kl.cdewrnxau4mv.us-east-2.rds.amazonaws.com:3306/gestion_de_stock?createDatabaseIfNotExist=true&userSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: kmerlinkAdmin
    password: kmerlinkdatabase$ql
    driver-class-name: com.mysql.cj.jdbc.Driver
  ## Specify DBMS
  jpa:
    database: MYSQL
    show-sql: false

    # Hibernate DDL Auto (create, create-drop, update)
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL5Dialect
    #Open in view
    open-in-view: false

server:
  port: 8081
